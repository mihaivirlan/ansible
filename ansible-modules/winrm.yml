- Create a new user on Windows machine
	- username: ansible
	- password: your_set_password
	- password hint: your_set_hint

- sudo yum install wget && sudo yum install gcc && sudo yum install python-devel && sudo yum install python-pip
  sudo dnf install wget && sudo dnf install gcc && sudo dnf install python3-devel && sudo dnf install python3-pip

- pip -V
- wget https://bootstrap.pypa.io/get-pip.py
- sudo python3.6 get-pip.py / sudo python get-pip.py
- pip install pywinrm

- From Windows machine:
- Create a new user and assign it to group Administrator
- From Start search -> Windows defender firewall -> Advanced Settings -> 
  Windows defender Firewall Propeties -> Firewall State -> 
  Inbound connections set as "Allow" for all tabs (Domain Profile/Private Profile/etc...)

- test from ansible-root machine: ping windows_hostname

- from ansible-root machine :
- sudo mkdir /etc/ansible/group_vars && sudo nano /etc/ansible/group_vars/win.yml
ansible_user: ansible
ansible_password: mihaivirlan1993
ansible_connection: winrm
ansible_winrm_transport: basic
ansible_winrm_port: 5985



- from windosws defender firewall -> advanced, from all tabs set the connections alllow

- from windows machine -> Start -> acces and open the terminal as a Administrator and set up a quick config:
  winrm quickconfig -q
  winrm set winrm/config/service @{AllowUnencrypted="true"}
  winrm set winrm/config/service/auth @{Basic="true"}

- from ansible-root machine:
ansible -m win_ping win